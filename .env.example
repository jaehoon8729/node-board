# =================================
# 환경변수 설정 파일 예시
# 이 파일을 .env로 복사하고 실제 값으로 변경하세요
# =================================

# =================================
# 데이터베이스 설정
# =================================
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_database_password
DB_NAME=node_board
DB_PORT=3306

# 데이터베이스 연결 풀 설정
DB_POOL_MAX=10
DB_POOL_MIN=0
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000

# =================================
# 서버 설정
# =================================
# 서버 포트
PORT=3000

# 환경 설정 (development, production, test)
NODE_ENV=development

# =================================
# 보안 설정
# =================================
# 세션 시크릿 키 (운영환경에서는 반드시 변경!)
SESSION_SECRET=change_this_to_random_string_in_production

# =================================
# 관리자 계정 설정
# =================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password

# =================================
# 파일 업로드 설정
# =================================
# 업로드 폴더 경로
UPLOAD_DIR=uploads
# 최대 파일 크기 (bytes) - 현재 50MB
MAX_FILE_SIZE=52428800
